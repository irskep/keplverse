{
  "scripts": {
    "dev": "parcel --port 1235 index.html",
    "build": "parcel build index.html --public-url .",
    "deploy": "parcel build index.html --public-url . && gh-pages -d dist"
  },
  "dependencies": {
    "jumbogrove": "^0.3.1",
    "stellardream": "^0.1.0",
    "style.css": "^1.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^9.3.1",
    "eslint": "^5.9.0",
    "eslint-config-google": "^0.11.0",
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.10.3",
    "postcss-import": "^12.0.1",
    "sass": "^1.14.3"
  }
}
